{{#each songs}}
<h3 class='song-title'>
    {{name}}
  <small class="artist-key">{{artist}}</small><small> on the album</small><small class="album-key"> {{album}}</small>
</h3>
<div class='.new-item delete_button'>
  <button class='btn btn-default btn-xs'>Delete</button>
</div>
{{/each}}